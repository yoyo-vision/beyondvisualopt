{
  "name": "beyondvisualopt",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && git add -A && git diff --staged --quiet || git commit -m '內容更新' && git push origin main && npx wrangler pages deploy dist --project-name=beyondvisualopt"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.3",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.1.6"
  }
}
